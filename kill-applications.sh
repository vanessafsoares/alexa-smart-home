#!/bin/sh

pkill -a visual\ studio\ code
pkill -a spotify
pkill -a slack
pkill -a google\ chrome
pkill -a microsoft\ outlook
pkill -a sqlpro\ studio
pkill -a android\ studio
pkill -a xcode
pkill -f terminal
sudo shutdown -h now